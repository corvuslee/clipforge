[project]
name = "clipforge"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
    "redis[hiredis]>=7.4.0",
]

[dependency-groups]
dev = [
    "fakeredis>=2.35.1",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
